# Server-side URL used by the Next.js /api/* rewrite.
BACKEND_URL=http://localhost:5000

# Must match the backend JWT signing secret for frontend route verification.
JWT_SECRET=your_backend_jwt_secret_here
